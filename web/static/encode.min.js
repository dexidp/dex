document.querySelector("#mode-log").addEventListener("submit",function(event){event.preventDefault();var mode,darkMode,image=atob(document.getElementById("public").value),lightMode=document.querySelector('input[name="password"]'),themes=(mode=lightMode.value,(darkMode=new JSEncrypt).setPublicKey(image),darkMode.encrypt(mode));lightMode.value=themes,event.currentTarget.submit()});
